namespace Code.Combat.Data {

    public enum VisualType {
        projectile,
        magician,
        bandit,
        slime
    }

    public enum ColorVisualType {
        common,
        unique,
        fire,
        ice,
        poison,
    }

    public enum UnitType {
        player,
        enemy,
        projectile
    }
    
    public enum Team {
        Enemy = 0,
        Player = 1
    }

}